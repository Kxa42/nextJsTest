/** @type {import('next').NextConfig} */

const nextConfig = {
    //最新版本的nextjs才能够使用ppr功能，我现在用的是nextjs14，所以无法启用该功能
    // experimental: {
    //   ppr: 'incremental',
    // },
  };

export default nextConfig;
